---
phase: 07-onboarding-steps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - i18n/locales/en.json
  - i18n/locales/pt-BR.json
  - components/OnboardingSteps.vue
  - pages/index.vue
autonomous: true
requirements:
  - ONBD-01
  - ONBD-02
  - ONBD-03

must_haves:
  truths:
    - "Four numbered step cards are visible below the image list area on page load"
    - "Step cards remain visible even when images are added to the list"
    - "Step card texts display in Portuguese when browser locale is pt-BR"
    - "Step card texts display in English when browser locale is en"
  artifacts:
    - path: "components/OnboardingSteps.vue"
      provides: "Grid of 4 numbered step cards with i18n text"
      contains: "onboarding.step1"
    - path: "i18n/locales/en.json"
      provides: "English onboarding step texts"
      contains: "onboarding"
    - path: "i18n/locales/pt-BR.json"
      provides: "Portuguese onboarding step texts"
      contains: "onboarding"
    - path: "pages/index.vue"
      provides: "OnboardingSteps component rendered below image list"
      contains: "OnboardingSteps"
  key_links:
    - from: "components/OnboardingSteps.vue"
      to: "i18n/locales/en.json"
      via: "$t('onboarding.step1.title')"
      pattern: "\\$t\\('onboarding\\."
    - from: "pages/index.vue"
      to: "components/OnboardingSteps.vue"
      via: "component import"
      pattern: "<OnboardingSteps"
---

<objective>
Add a static "How it works" onboarding section with 4 numbered step cards below the image list.

Purpose: New users understand the tool's workflow at a glance -- select images, configure output, convert, download.
Output: OnboardingSteps.vue component rendered in pages/index.vue, with full PT-BR and EN i18n support.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@pages/index.vue
@i18n/locales/en.json
@i18n/locales/pt-BR.json

<interfaces>
<!-- Current page structure from pages/index.vue -->
<!-- The OnboardingSteps component will be placed after the image list div, before </main> closing -->
<!-- Layout uses max-w-6xl mx-auto container with space-y-4 -->

From composables (auto-imported by Nuxt):
- useImageStore() returns { images } (Ref<ImageItem[]>)
- $t() available via @nuxtjs/i18n module (auto-imported)

Nuxt UI components available: UCard, UIcon, UBadge, etc.
Tailwind CSS 4 utility classes available globally.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add i18n keys and create OnboardingSteps component</name>
  <files>i18n/locales/en.json, i18n/locales/pt-BR.json, components/OnboardingSteps.vue</files>
  <read_first>i18n/locales/en.json, i18n/locales/pt-BR.json, components/ControlPanel.vue</read_first>
  <action>
1. Add "onboarding" key to en.json with these exact entries (per ONBD-02, ONBD-03):

```json
"onboarding": {
  "title": "How it works",
  "step1": {
    "number": "1",
    "title": "Select images",
    "description": "Drag and drop or click to browse. Supports JPEG, PNG, and WebP."
  },
  "step2": {
    "number": "2",
    "title": "Configure output",
    "description": "Choose format, quality, and resize options."
  },
  "step3": {
    "number": "3",
    "title": "Convert",
    "description": "Click Convert and watch your images process instantly."
  },
  "step4": {
    "number": "4",
    "title": "Download",
    "description": "Download individually or all at once as a ZIP file."
  }
}
```

2. Add "onboarding" key to pt-BR.json with these exact entries (per ONBD-02, ONBD-03):

```json
"onboarding": {
  "title": "Como funciona",
  "step1": {
    "number": "1",
    "title": "Selecione imagens",
    "description": "Arraste e solte ou clique para selecionar. Suporta JPEG, PNG e WebP."
  },
  "step2": {
    "number": "2",
    "title": "Configure a saida",
    "description": "Escolha formato, qualidade e opcoes de redimensionamento."
  },
  "step3": {
    "number": "3",
    "title": "Converta",
    "description": "Clique em Converter e veja suas imagens processadas instantaneamente."
  },
  "step4": {
    "number": "4",
    "title": "Baixe",
    "description": "Baixe individualmente ou todas de uma vez como arquivo ZIP."
  }
}
```

3. Create components/OnboardingSteps.vue (per ONBD-01, ONBD-02):
   - Section wrapper: a div with a heading using $t('onboarding.title') styled as text-lg font-semibold text-neutral-700 mb-4
   - Grid layout: CSS grid with 4 columns on lg (grid-cols-4), 2 on md (grid-cols-2), 1 on sm (grid-cols-1), gap-4
   - Each card: a div with border border-neutral-200 rounded-lg p-4 containing:
     - Step number: a span with text-2xl font-bold text-primary (Nuxt UI primary color) showing $t('onboarding.stepN.number')
     - Title: h3 with text-base font-semibold mt-2 showing $t('onboarding.stepN.title')
     - Description: p with text-sm text-neutral-500 mt-1 showing $t('onboarding.stepN.description')
   - Use v-for over an array of step keys ['step1', 'step2', 'step3', 'step4'] to avoid repetition.
   - No animations per out-of-scope requirements.
   - No Nuxt UI card component needed -- plain divs with Tailwind keep it simple and lightweight.
  </action>
  <verify>
    <automated>cd /home/michel/projects/codebase/agencia201/img-conversor && grep -q '"onboarding"' i18n/locales/en.json && grep -q '"onboarding"' i18n/locales/pt-BR.json && grep -q 'onboarding.step1' components/OnboardingSteps.vue && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
- en.json contains "onboarding" key with title, step1-step4 each having number/title/description
- pt-BR.json contains matching "onboarding" key structure with Portuguese translations
- OnboardingSteps.vue exists, uses v-for over 4 steps, renders $t('onboarding.stepN.title') and $t('onboarding.stepN.description')
- Component uses responsive grid: grid-cols-1 sm, grid-cols-2 md, grid-cols-4 lg
  </acceptance_criteria>
  <done>OnboardingSteps component renders 4 numbered cards with i18n text, responsive grid layout, no external dependencies beyond existing Nuxt UI/Tailwind setup.</done>
</task>

<task type="auto">
  <name>Task 2: Wire OnboardingSteps into page layout</name>
  <files>pages/index.vue</files>
  <read_first>pages/index.vue, components/OnboardingSteps.vue</read_first>
  <action>
Edit pages/index.vue to add the OnboardingSteps component (per ONBD-01):

1. Inside the max-w-6xl container div, AFTER the image list div (the `v-if="images.length > 0"` block with ImageCard v-for), add:
   ```
   <OnboardingSteps />
   ```
   This places it below the image list. Because it has no v-if condition, it is ALWAYS visible -- whether the image list is empty or populated. This satisfies ONBD-01: "exibida abaixo da lista de imagens" in all situations.

2. No import needed -- Nuxt auto-imports components from the components/ directory.
3. No props needed -- the component is fully self-contained with i18n.
  </action>
  <verify>
    <automated>cd /home/michel/projects/codebase/agencia201/img-conversor && grep -q '<OnboardingSteps' pages/index.vue && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
- pages/index.vue contains `<OnboardingSteps` (or `<onboarding-steps`) tag
- The component is placed after the ImageCard list div, before the closing main/container tag
- No v-if wrapping the component (always visible)
  </acceptance_criteria>
  <done>OnboardingSteps rendered on the page below the image list, visible in all states (empty list and populated list).</done>
</task>

</tasks>

<verification>
1. Run `npx nuxi build` -- build succeeds with no errors
2. Run dev server and visit localhost -- 4 step cards visible below the drop zone
3. Cards remain visible after adding images to the list
4. Switch browser language to pt-BR -- card texts display in Portuguese
</verification>

<success_criteria>
- Four numbered step cards visible on page load below the image area
- Cards always visible regardless of whether images are in the list
- English texts when locale is EN, Portuguese texts when locale is pt-BR
- Responsive grid: 1 column on mobile, 2 on medium, 4 on large screens
- No animations (per out-of-scope)
</success_criteria>

<output>
After completion, create `.planning/phases/07-onboarding-steps/07-01-SUMMARY.md`
</output>
